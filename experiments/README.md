# Experiments

Use this directory for short-lived AI experiments:

- prompt and model eval scripts
- notebook prototypes
- quick benchmark runs

Promote successful experiments into `services/` or `apps/`.
